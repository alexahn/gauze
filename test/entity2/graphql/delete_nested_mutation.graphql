mutation DeleteEntity2NestedMutation($id1: String, $id2: String) {
	delete_entity2(where: { id: $id1 }) {
		attributes {
			id
			text
		}
		mutation {
			delete_entity2(where: { id: $id2 }) {
				attributes {
					id
					text
				}
			}
		}
	}
}
