mutation DeleteYtitne(
	$where: Ytitne_Mutation__Attributes
	$where_in: Ytitne_Mutation__Attributes_Array
	$where_not_in: Ytitne_Mutation__Attributes_Array
	$limit: Int
	$offset: Int
	$order: String
	$order_direction: String
	$order_nulls: String
) {
	delete_ytitne(
		where: $where
		where_in: $where_in
		where_not_in: $where_not_in
		limit: $limit
		offset: $offset
		order: $order
		order_direction: $order_direction
		order_nulls: $order_nulls
	) {
		attributes {
			id
			created_at
			updated_at
			deleted_at
			text
		}
	}
}
