mutation DeleteRelationship($where: Relationship_Mutation__Attributes) {
    delete_relationship(where: $where) {
        attributes {
			_id
			_created_at
			_updated_at
			_deleted_at
			_from_type
			_from_id
			_to_type
			_to_id
        }
    }
}
