class RealmSystemController {
	constructor() {}
	enter_session(context, scope, parameters) {
		// use realm helper function here
	}
	exit_session(context, scope, parameters) {
		// use realm helper function here
	}
}

const CONTROLLER__REALM_SYSTEM__CONTROLLER__ENVIRONMENT = new RealmSystemController();

export { CONTROLLER__REALM_SYSTEM__CONTROLLER__ENVIRONMENT };
