class RealmDatabaseController {
	constructor() {}
	enter_session(context, scope, parameters) {
		// use realm helper function here
	}
	exit_session(context, scope, parameters) {
		// use realm helper function here
	}
}

const CONTROLLER__REALM_DATABASE__CONTROLLER__ENVIRONMENT = new RealmDatabaseController();

export { CONTROLLER__REALM_DATABASE__CONTROLLER__ENVIRONMENT };
